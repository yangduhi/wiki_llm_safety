---
name: marp-publish
description: Export materialized overview notes to slides using Marp when the CLI is installed locally.
---

# Marp publish

- Check `.\scripts\wiki.ps1 slides` first.
- Export only from materialized overview notes, not arbitrary raw pages.
- If Marp is missing, stop with the documented graceful fallback message.
