---
name: source-authority-audit
description: Audit `sources/authority_registry.yaml`, `sources/document_inventory.csv`, and authoritative raw snapshots before ingest or policy work.
---

# Source authority audit

- Read `ADR-0003` first.
- Use `python -m wiki_obsidian.cli source-audit`.
- Keep `official_web` separate from convenience clip intake.
