---
name: web-clip-intake
description: Intake clipped markdown or HTML into `raw/collections/web_clipper/` and prepare it for scan or ingest.
---

# Web clip intake

- Save captured files under `raw/collections/web_clipper/`.
- Do not treat clip intake as authoritative by default.
- Run scan or ingest only after the file is in the repo.
