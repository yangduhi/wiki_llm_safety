# Phase 5 - MCP / Agent / Skill Wiring

## Goal

Connect repo-local commands, MCP contracts, agent roles, and skills to the v2 repository contract.

## Deliverables

- `.codex` role definitions
- `.agents/skills`
- `configs/mcp/servers.yaml`

## Done When

- every role documents `evidence`, `uncertainty`, `target outputs`, `validation command`, and `classification impact`
