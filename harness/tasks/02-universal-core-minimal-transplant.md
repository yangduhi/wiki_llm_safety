# Task 02 - Universal Core Minimal Transplant

1. Ensure the package name is `wiki_obsidian`.
2. Confirm reusable source, script, and harness files are in place.
3. Run a single collection through `scan`, `parse`, `normalize`, and `ingest`.

## Done when

- the minimal reusable core runs without the old package contract
