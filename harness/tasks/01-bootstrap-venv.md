# Task 01 - Bootstrap + venv

1. Run `.\scripts\bootstrap.ps1`.
2. Run `.\.venv\Scripts\python.exe -m wiki_obsidian.cli --help`.
3. Run `.\scripts\wiki.ps1 verify`.

## Done when

- the environment is installed and verify passes
